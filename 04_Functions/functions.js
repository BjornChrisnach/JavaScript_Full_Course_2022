/*
var x = 10,
  y = 5,
  total = x + y;

document.write(total);
*/

/*
function sum(x, y) {
  var total = x + y;

  document.write(total);
}

// sum(4, 3);
sum("Welcome to ", "Simplicode");
*/

function sum(x, y) {
  var total = x + y;
  return total;
}

var output = sum(35, 15);
document.write(output);
